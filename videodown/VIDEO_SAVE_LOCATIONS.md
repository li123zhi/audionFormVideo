# 📁 视频保存位置说明

## 自动保存位置

当您使用"迭代调整剪辑"功能生成新视频时，视频会自动保存到以下位置：

### 1. 任务目录（主要）
```
videorecomp/backend/tasks/<任务ID>/output/timeline_remapped_video.mp4
```

**示例：**
```
videorecomp/backend/tasks/86bf3cf0-d8e7-4081-9037-06077190d800/output/timeline_remapped_video.mp4
```

### 2. 全局输出目录（备份）
```
output/adjusted_<任务ID前8位>_video.mp4
```

**示例：**
```
output/adjusted_86bf3cf0_video.mp4
```

---

## 如何获取生成的视频

### 方法1：通过Web界面下载（推荐）⭐

1. 在处理完成后，点击"下载视频"按钮
2. 视频会自动下载到您的浏览器下载目录

### 方法2：直接从文件系统访问

1. **任务目录：**
   ```bash
   cd videorecomp/backend/tasks
   ls -la */output/timeline_remapped_video.mp4
   ```

2. **全局输出目录：**
   ```bash
   cd output
   ls -la adjusted_*.mp4
   ```

### 方法3：查看处理日志

日志文件会记录详细的保存位置：
```
videorecomp/backend/tasks/<任务ID>/output/timeline_remap_log.json
```

---

## 日志文件

每个任务都会生成详细的处理日志，包含：

- **匹配统计**：哪些字幕匹配成功
- **时间信息**：原视频时长、新视频时长
- **提取记录**：每个片段从哪里提取
- **保存路径**：视频文件的具体位置

**日志位置：**
```
videorecomp/backend/tasks/<任务ID>/output/timeline_remap_log.json
```

---

## 命令行查看示例

### 查看最近生成的视频
```bash
cd /Users/ruite_ios/Desktop/aiShortVideo/videorecomp/videodown

# 查看全局输出目录的视频
ls -lht output/adjusted_*.mp4 | head -5

# 查看任务目录的视频
find videorecomp/backend/tasks -name "timeline_remapped_video.mp4" -type f | head -5
```

### 查看某个任务的日志
```bash
cat videorecomp/backend/tasks/<任务ID>/output/timeline_remap_log.json | jq '.stats'
```

---

## 视频命名规则

- **任务目录**：`timeline_remapped_video.mp4`（固定名称）
- **全局目录**：`adjusted_<任务ID前8位>_video.mp4`（唯一名称）

---

## 清理旧视频

### 清理全局输出目录
```bash
cd output
rm adjusted_*.mp4
```

### 清理特定任务
```bash
rm -rf videorecomp/backend/tasks/<任务ID>
```

### 清理所有已完成任务
```bash
cd videorecomp/backend/tasks
# 只保留最近7天的任务
find . -maxdepth 1 -type d -mtime +7 -exec rm -rf {} \;
```

---

## 提示

✅ **推荐**：通过Web界面下载，方便快捷
✅ **备份**：视频会同时保存在两个位置
✅ **日志**：查看日志了解详细的处理过程
✅ **清理**：定期清理旧视频以节省磁盘空间
